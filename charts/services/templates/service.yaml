apiVersion: v1
kind: Service
metadata:
  name: {{ include "charts.services.fullname" . }}
  labels:
    {{- include "charts.services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "charts.services.selectorLabels" . | nindent 4 }}